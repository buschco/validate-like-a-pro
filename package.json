{
  "name": "validate-like-a-pro",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "validate": "yarn workspace validate",
    "app": "yarn workspace app",
    "backend": "yarn workspace backend"
  }
}
